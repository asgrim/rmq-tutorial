Bunny+React serverless chat client
==================================

 * Use Bunny + React to make a *serverless* chat client
   * (not stictly serverless, RabbitMQ is the server...)
 * `composer require clue/stdio-react`
 * Create `client.php`
